apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio
  namespace: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio
  template:
    metadata:
      labels:
        app: portfolio
    spec:
      containers:
      - name: portfolio
        image: myportfolioacr.azurecr.io/portfolio:v1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000